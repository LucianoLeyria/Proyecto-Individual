return (

    { Loading == false (mostrame loader ):(

        {
            currentVideogames ? (render tarjetas) : (error de no existe)
        }

    )  

    }



primer if -----> cartelito de cargando
        -------> if -------------> render       
                 -----------> error
)